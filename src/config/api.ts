// 在頁面中直接使用：API_BASE_URL + "/api/home"

// 確保始終使用 HTTPS，防止 Mixed Content 錯誤
// 重要：必須使用 https:// 協議，因為 GitHub Pages 使用 HTTPS
export const API_BASE_URL = "https://atelierenclave-backend.zeabur.app";

// 開發環境可以使用 localhost（取消註解下面這行，並註解上面那行）
// export const API_BASE_URL = "http://localhost:3000";

